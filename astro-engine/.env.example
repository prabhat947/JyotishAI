# CORS: comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# Environment: "development" or "production"
# In production, Swagger UI (/docs, /redoc) is disabled
ENVIRONMENT=development
